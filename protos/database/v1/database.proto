syntax = "proto3";

package database;

service Database {
  rpc GetDatabases (Connection) returns (Databases) {}
}

message Connection {
  string connectionString = 1;
}

message Databases {
  repeated string database = 1;
}